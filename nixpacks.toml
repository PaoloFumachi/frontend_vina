[phases.setup]
nixPkgs = ["nodejs-22_12", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build -- --configuration=production"]

[start]
cmd = "npx http-server dist/my-app-dsi6/browser -p $PORT"